# ============================================
# CONFIGURACIÓN DE RIDEUPT BACKEND
# ============================================
# IMPORTANTE: Copia este archivo a .env y rellena con tus valores reales
# ============================================

# ============================================
# MONGODB - BASE DE DATOS
# ============================================
# Para MongoDB Atlas (Cloud - RECOMENDADO PARA PRODUCCIÓN):
# MONGO_URI=mongodb+srv://usuario:password@cluster.xxxxx.mongodb.net/rideupt?retryWrites=true&w=majority&appName=RideUpt

# Para MongoDB Local (Solo desarrollo):
# MONGO_URI=mongodb://localhost:27017/rideupt

# Para MongoDB en Docker:
# MONGO_URI=mongodb://mongo-dev:27017/rideupt

MONGO_URI=mongodb+srv://TU_USUARIO:TU_PASSWORD@TU_CLUSTER.mongodb.net/rideupt?retryWrites=true&w=majority&appName=RideUpt

# ============================================
# JWT - AUTENTICACIÓN
# ============================================
# Cambia esto por una clave secreta segura y única
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=TU_CLAVE_SECRETA_SUPER_SEGURA_AQUI

# ============================================
# SERVIDOR
# ============================================
PORT=3000
NODE_ENV=production

# ============================================
# FIREBASE (Para notificaciones push)
# ============================================
# La configuración de Firebase se carga desde config/firebase-service-account.json
# Asegúrate de tener ese archivo en su lugar

# ============================================
# CORS (Opcional - para desarrollo)
# ============================================
# CORS_ORIGIN=https://tu-dominio.com

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NUNCA subas el archivo .env con valores reales a Git
# 2. En producción, usa variables de entorno del servidor
# 3. Para MongoDB Atlas:
#    - Crea una cuenta en https://cloud.mongodb.com
#    - Crea un cluster gratuito (M0)
#    - Configura un usuario de base de datos
#    - Agrega tu IP a la whitelist (o usa 0.0.0.0/0 para permitir todas)
#    - Copia la connection string y reemplaza <password> con tu contraseña real
# 4. Cambia JWT_SECRET por un valor único y seguro







